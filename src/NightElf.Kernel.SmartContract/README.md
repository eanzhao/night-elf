# NightElf.Kernel.SmartContract

Smart contract execution primitives for NightElf.

Current scope:

- tiered state cache and cached state reader
- sync fast-path state access
- `SmartContractExecutor` entrypoint for generated contract dispatch

Placeholder directory reserved for the contract execution engine.
